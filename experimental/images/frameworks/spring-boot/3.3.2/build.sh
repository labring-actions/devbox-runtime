#!/usr/bin/env bash
set -euo pipefail

# Spring Boot framework is installed via Maven, no additional installation needed
# This build.sh is kept for consistency with the architecture
# If you need to install spring-boot-specific tools or configure spring-boot-related settings,
# you can add them here

echo "Spring Boot 3.3.2 framework image - no additional installation required"
echo "Spring Boot framework will be installed via Maven when building the project"


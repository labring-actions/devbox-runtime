#!/usr/bin/env bash
set -euo pipefail

# Django framework is installed via pip, no additional installation needed
# This build.sh is kept for consistency with the architecture
# If you need to install django-specific tools or configure django-related settings,
# you can add them here

echo "Django 4.2.16 framework image - no additional installation required"
echo "Django framework will be installed via pip when building the project"


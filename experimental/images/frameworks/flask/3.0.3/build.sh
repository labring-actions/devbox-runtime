#!/usr/bin/env bash
set -euo pipefail

# Flask framework is installed via pip, no additional installation needed
# This build.sh is kept for consistency with the architecture
# If you need to install flask-specific tools or configure flask-related settings,
# you can add them here

echo "Flask 3.0.3 framework image - no additional installation required"
echo "Flask framework will be installed via pip when building the project"

